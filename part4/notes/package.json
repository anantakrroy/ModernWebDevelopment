{
  "name": "notes",
  "version": "1.0.0",
  "description": "notes app using node http module",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../../part2/notes && npm run build && @powershell Copy-Item dist -Recurse ../../part4/notes",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix"
  },
  "keywords": [
    "nodejs",
    "http"
  ],
  "author": "anantakrroy",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.1.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  }
}
